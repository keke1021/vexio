DATABASE_URL="postgresql://user:password@localhost:5432/vexio?schema=public"
JWT_SECRET="your-super-secret-jwt-key"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_EXPIRES_DAYS=7
PORT=3001

# Orígenes permitidos para CORS, separados por coma (sin trailing slash).
# Si no se setea, cae a http://localhost:5173,http://localhost:3000 (dev).
# En producción: ALLOWED_ORIGINS="https://app.tudominio.com,https://tudominio.com"
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"
